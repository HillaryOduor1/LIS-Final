/* eslint-disable no-unused-vars */
// Service worker will be generated by workbox build
self.addEventListener('install', function(event) {
  self.skipWaiting();
});
self.addEventListener('fetch', function(event) {
  // Network-only for now
});